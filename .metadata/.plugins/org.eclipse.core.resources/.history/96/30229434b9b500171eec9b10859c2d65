package de.bachd.bigdata;

public class QueryBuilder {

	public static QueryBuilder select(String... strings) {
		QueryNode node = new QueryNode("SelectNode");
		Query.addNode(node);
		return QueryBuilder;
	}

}
